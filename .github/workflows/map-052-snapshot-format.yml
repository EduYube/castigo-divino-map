name: MAP-052 snapshot format helper

on:
  push:
    branches:
      - agent/map-052-snapshot-sync
    paths:
      - .github/workflows/map-052-snapshot-format.yml

permissions:
  contents: write

jobs:
  format:
    name: Format and verify current public snapshot
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout snapshot sync branch
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          ref: agent/map-052-snapshot-sync

      - name: Set up Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Format snapshot
        run: npx prettier --write public/data/public-catalog.snapshot.json

      - name: Verify local snapshot contract
        run: npm run snapshot:verify

      - name: Verify formatted snapshot against Supabase
        env:
          VITE_SUPABASE_URL: https://ehpouvbzmvwbkkoypgfa.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY: sb_publishable_b5Kx8EpNWtRvUHSvHPrQIA_pdOZT7p0
        run: npm run snapshot:verify:remote

      - name: Commit formatted snapshot
        run: |
          if git diff --quiet -- public/data/public-catalog.snapshot.json; then
            echo "Snapshot already formatted."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/data/public-catalog.snapshot.json
          git commit -m "chore: format synchronized public snapshot"
          git push origin HEAD:agent/map-052-snapshot-sync
